Config = {}

-- Language (locale file name from locales/ folder)
Config.Locale = 'en'

-- Keybind to open/close the FPS menu
Config.OpenKey = 'F7'

-- Command to open/close the FPS menu
Config.Command = 'fpsmenu'

-- Maximum saved profiles per player (database-backed)
Config.MaxProfiles = 5

-- Show FPS counter in top-right corner
Config.ShowFPSCounter = true

-- Disable GTA dispatch services (police/ambulance AI dispatch)
-- Significant CPU savings, recommended for RP servers that handle this via script
Config.DisableDispatch = true

-- Check for updates on server start via GitHub Releases API
Config.VersionCheck = true
Config.GitHubRepo = 'F5-Studio-FiveM/f5_boost'
